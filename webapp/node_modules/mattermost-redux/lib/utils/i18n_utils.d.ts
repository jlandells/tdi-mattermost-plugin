type LocalizeFunc = (descriptor: {
    id: string;
    defaultMessage?: string;
}) => string;
export declare function setLocalizeFunction(func: LocalizeFunc): void;
export declare function localizeMessage(descriptor: {
    id: string;
    defaultMessage?: string;
}): string;
export {};

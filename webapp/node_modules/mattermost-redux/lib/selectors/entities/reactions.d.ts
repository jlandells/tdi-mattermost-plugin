import type { GlobalState } from '@mattermost/types/store';
export declare function canAddReactions(state: GlobalState, channelId: string): boolean;
export declare function canRemoveReactions(state: GlobalState, channelId: string): boolean;
